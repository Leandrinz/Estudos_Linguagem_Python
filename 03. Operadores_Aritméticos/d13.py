# Recebe salário atual
sa = float(input('Digite seu salário atual: '))
# Calculo pro aumento do salário
sr = sa + (sa * 0.15)
# Exibe resultado do novo salário
print('O seu salário pós reajuste ficou igual a {}'.format(sr))